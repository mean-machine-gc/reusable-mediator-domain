{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "command": "bash -c 'if echo \"$TOOL_INPUT\" | grep -q \"\\.spec\\.ts\"; then npm run gen:specs 2>&1 | tail -5 || true; fi'"
      }
    ]
  }
}
